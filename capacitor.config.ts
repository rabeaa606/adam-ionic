import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.adamBarber',
  appName: 'Adam',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
